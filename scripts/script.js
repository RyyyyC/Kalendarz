window.onresize = () => {
    // months = document.getElementsByClassName("month");
    // Object.values(months).forEach(element => {
    //     element.style.cssText += `width: ${document.getElementsByTagName("body")[0].offsetWidth / 5}px;`
    //     element.style.cssText += `height: ${document.getElementsByTagName("html")[0].offsetHeight / 3}px;`
    // });
    // month.forEach(element => {
    //    console.log(element) 
    // });
}